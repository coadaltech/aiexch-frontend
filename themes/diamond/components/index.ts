/**
 * Diamond theme design-system — reusable, presentational building blocks for
 * bespoke Diamond market views. They consume the shared odds/color tokens, so
 * they need no logic and stay in sync with theme + admin color customization.
 */
export { OddsButton } from "./odds-button";
export type { OddsButtonProps } from "./odds-button";
export { MarketCard } from "./market-card";
export type { MarketCardProps, MarketRunner } from "./market-card";
